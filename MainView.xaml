<UserControl x:Class="PlayniteTheme.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>

            <Grid.Background="#1e1e1e">

                <Image Source="{Binding SelectedGame.BackgroundImage}"
                          Stretch="UniformToFill"
                          Opacity="1"/>

            <Button Name="PlayButton" Width="200" Height="60" FontSize="18" FontWeight="Bold">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Content" Value="Install"/>
                        <Setter Property="Background" Value="#007ACC"/>
                        <Setter Property="Foreground" Value="White"/>

                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsInstalled}" Value="True">
                                <Setter Property="Content" Value="Play"/>
                                <Setter Property="Background" Value="#2ea44f"/>
                            </DataTrigger>

                            <DataTrigger Binding="{Binding IsRunning}" Value="True">
                                <Setter Property="Content" Value="Running"/>
                                <Setter Property="Background" Value="#ff8c00"/>
                            </DataTrigger>
                        </Style.Triggers>    
                    </Style>
                </Button.Style>
            </Button>
            
            <StackPanel Margin="0,150,0,0" HorizontalAlignment="Center" VerticalAlignment="Top">
                <TextBlock Text="{Binding SelectedGame.Name}"
                            FontSize="24"
                            Foreground="White"
                            FontWeight="Bold"
                            HorizontalAlignment="Center"/>
                
                <TextBlock Text="{Binding SelectedGame.Playtime, StringFormat='Time Played: {0} hours'}"
                            FontSize="14"
                            Foreground="#aaaaaa"
                            HorizontalAlignment="Center"/>
            </StackPanel>

    </Grid> 
</UserControl>